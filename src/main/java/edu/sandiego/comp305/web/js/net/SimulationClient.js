// WebSocket client that talks to the Java SimulationServer

export class SimulationClient {
    constructor(url) {
    }

    connect() {
    }

    onState(callback) {
    }

    sendCommand(command) {
    }
}
